import React from 'react';

export default ({label}) => (
    <div style={{background: "#DEDEDE", borderRadius: "900px", padding: "2px 8px", fontSize: "12px", display: "inline-block", marginRight:"8px", marginBottom: "8px"}}> {label} </div>
)